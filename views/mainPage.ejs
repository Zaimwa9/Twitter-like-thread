<% if(comments.length >0) { %>
    <% for(i=0; i< comments.length; i++){ %>
      <div  style="border-style: solid ; border-width: 1px">
        <p> <%= comments[i].content %> </p>
        <p> <%= comments[i].likesArray.length%>  <i> <sub> By <%=comments[i].username %> on <%=moment(comments[i].created_at).format('DD/MM/YY h:mm:ss') %> </sub> </i> 
        <form method="post" action="/addLike/<%= comments[i]._id %>">
        <button> Like </button>
        </form>
        <form method="post" action="/removeLike/<%= comments[i]._id  %>">
        <button> Unlike </button>
        </form>
      </div>
    <% } %>
<% } %>

</br>
</br>

<form method="post" action="/newComment">                      
     <textarea  id="comment" input type="text" name="comment" style="width:648px; height:120px" required></textarea> <br>
      <button class="form-btn semibold" type="submit" value="submit_com"> Afflutweet it </button>
</form>
